/**
 * REMOVED in the 2026-09-02 reconstruction (see docs/RECONSTRUCTION.md §F).
 * The NPC errand ledger was simulation without gameplay. This file is an
 * empty placeholder only because the sync tool cannot delete files on disk —
 * delete it.
 */
export {};
