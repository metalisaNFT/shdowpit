import { describe, it, expect } from 'vitest';

// REMOVED in the 2026-09-02 reconstruction — the NPC quest ledger no longer
// exists. This placeholder only exists because the sync tool cannot delete
// files on disk. Delete it.
describe('NpcQuests (removed)', () => {
  it('is gone', () => {
    expect(true).toBe(true);
  });
});
